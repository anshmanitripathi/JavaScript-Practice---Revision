//  trim()  trim the whitespaces from both the ends of string


// Strings are unmutable hence always a new string created when manpulated.


let str = "     This is a ball, i want to play.      ";

let strNew = str.trim();

console.log(strNew);